- content_for :head do
  %link{:rel => 'stylesheet', :href => '/stylesheets/tag.css', :type => 'text/css', :media => 'all'}

#content
  %h1 Specific Tag
  %p This functionality will be available in a future implementation
  - @users.each do |user|
    %p= user.slug